一. 选择题

1-5.   b c b b c

6-10.  b c b b d

11-15  c d b a c

16-20  b c d a (aab)

21-25  (dd) (ac) a (bab) c

26-30  d a (bc) b a



二. 填空题

1.  Number()  parselnt()  parseFloat()
2.  children()  paresNode()
3.  split()  indexOf()
4.  random()
5. setInterval()  setTimeout()  clearTimeout()和clearInterval()
6. focus()  blur()



三. 代码题

文件中...



四. 简答题

1.

apply()与call()的含义一样，均为改变调用函数中的this指向。
其中apply()与call()的第一个参数表示所要指向的对象，若调用函数无参数可不写，则默认为window。第一个参数后的参数表示调用函数的参数，其中apply()第一个参数后面的参数为一个数组，call()第一个参数后面为0或多个参数。



2.

原型链: js在创建对象（不论是普通对象还是函数对象）的时候，都有一个_proto_的内置属性，指向创建它的函数对象的原型。

闭包: 闭包是一种程序结构，即内部函数能够引用外部函数的参数和局部变量，当外部函数返回内部函数时，相关参数和变量都保存在返回的函数中



3.

Promise 是异步编程的一种解决方案： 从语法上讲，promise是一个对象，从它可以获取异步操作的消息；从本意上讲，它是承诺，承诺它过一段时间会给你一个结果。 promise有三种状态： pending(等待)，fulfiled(成功)，rejected(失败)；状态一旦改变，就不会再变。创造promise实例后，它会立即执行.

同步的方式写异步的代码，用来解决回调地狱问题。

此外，promise对象提供统一的接口，使得控制异步操作更加容易



4.

普通函数: 

1. this总是代表它的直接调用者, 例如 obj.func ,那么func中的this就是obj

2. 调用的时候，没有任何前缀，则指向window，new的时候，指向new出来的对象。

   3.在默认情况(非严格模式下,未使用 'use strict'),没找到直接调用者,则this指的是 window

   4.在严格模式下,没有直接调用者的函数中的this是 undefined

   5.使用call,apply,bind(ES5新增)绑定的,this指的是 绑定的对象 

箭头函数:

   默认指向在定义它时,它所处的对象,而不是执行时的对象, 定义它的时候,可能环境是window（即继承父级的this）



5.

1. var声明的变量会挂载在window上，而let和const声明的变量不会
2. var声明变量存在变量提升，let和const不存在变量提升
3. let和const声明形成块作用域
4. 同一作用域下let和const不能声明同名变量，而var可以



6.

新增特性: 

1. const 与 let 变量

2. 模板字面量

3. 解构

4. 对象字面量简写法
5. for…of循环
6. 展开运算符
7. 箭头函数和this
8. Javascript类
9. super 和 extends

新增数组方法:

1. forEach()会遍历数组, 循环体内没有返回值, forEach()循环不会改变原来数组的内容
2. map()的主要作用, 其实是创建一个新的数组,  使用map()遍历数组, 可以返回一个新数组, 并且不会改变原数组里的内容
3. filter()主要是过滤的, 用来过滤数组中不满足条件的元素, 把满足条件的元素放到新的数组里, 并且不会改变原数组.
4. every()的作用, 它会遍历数组, 在循环体内写条件, 如果每一项都是true, 就会返回true, 只要有一个是false, 就会返回false
5. some()遍历数组的每一项, 然后根据循环体内的条件去判断, 只要有一个是true, 就会停止循环.
6. reduce()实现数字累加



7.

区别:

前者仅判断数据的值，而后者是先判断数据的类型，相同的前提下在判断数据的值。

数据类型:  

Undefined、Null、Boolean、Number、String、Symbol（es6新增，表示独一无二的值 ) 和 BigInt（es10新增）



8.

事件委托:

把原本需要绑定在子元素的响应事件委托给父元素，让父元素担当事件监听的职务。



9.

1.null表示没有对象，即该处不应该有值

2.undefined表示缺少值，即此处应该有值，但没有定义



10.

1.语义化标签    2.语义化标签   3.新增视频 <video> 和音频 <audio> 标签   4.过渡   5.动画   



11.

jsonp是script标签不受跨域限制,后端是返回的函数调用,再使用同名函数接收就行,但局限于get请求

cors是利用浏览器和服务器传递的机制,但需要后端设置cors接口才能使用



12.

原理:

Ajax的工作原理相当于在用户和服务器之间加了—个中间层(AJAX引擎)，使用户操作与服务器响应异步化。并不是所有的用户请求都提交给服务器。像—些数据验证和数据处理等都交给Ajax引擎自己来做,，只有确定需要从服务器读取新数据时再由Ajax引擎代为向服务器提交请求。

流程:

1. 创建XMLHttpRequest
2. 设置请求方式
3. 调用回调函数
4. 发送请求